class Solution {
  static function main() {
    var inputFile = Sys.args()[0];
    var part = Sys.args()[1];
    var input = sys.io.File.getContent(inputFile);
    if (part == "1") {
      trace("Part 1 not yet implemented");
    } else {
      trace("Part 2 not yet implemented");
    }
  }
}
